/**
 * Route name constants
 * Use these instead of raw strings to avoid typos.
 */

export const Routes = {
    SPLASH: 'Splash',
    LOGIN: 'Login',
    REGISTER: 'Register',
    OTP: 'OTP',
    HOME: 'Home',
    PACKAGE_DETAIL: 'PackageDetail',
    CHECKOUT: 'Checkout',
    PAYMENT: 'Payment',
} as const;
